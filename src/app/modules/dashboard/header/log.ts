export type LoginOrLogout = 'login' | 'logout';

export enum LoginOrLogoutEnum {
    Login = 0,
    Logout,
}

export enum LoginOrLogoutEnum_En {
    'login' = LoginOrLogoutEnum.Login,
    'logout' = LoginOrLogoutEnum.Logout,
}
